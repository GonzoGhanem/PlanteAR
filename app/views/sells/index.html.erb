<h1>Ventas</h1>
<h3>Mostrando: <small>Todas</small></h3> 
<div class='form-inline'>
<a id="sells-today">Solo hoy</a> || 
<a id="sells-yesterday">Solo ayer</a> || 
	<%= label :day, "Dia" %>
	<%= text_field :day, {}, {:id => "date_day", :placeholder => "Dia",:class => 'input-mini'} %>
	<%= label :month, "Mes" %>
	<%= select_month(Date.today,{},:class => 'input-medium') %>
	<%= label :year, "Año" %>
	<%= select_year(Date.today,{},:class => 'input-mini') %>
	<a id="custom-filter" class="btn btn-success">Filtrar</a> ||
<a id="sells-remove"> Remover filtros</a> 
</div>
<br></br>
<table id="sells"class="table table-striped table-bordered" data-source="<%= sells_url(format: "json") %>">
 <thead>
  <tr>
      <th>Medio de pago</th>
      <th>Monto</th>
      <th>Fecha</th>
    <th></th>
  </tr>
 </thead>
 <tbody>
  </tbody>

</table>

<br></br>

<%= link_to 'Nueva Venta', new_sell_path, :class=>"btn btn-success" %>
